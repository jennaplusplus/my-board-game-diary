<div class="page-header">
  <h1>Meetups</h1>
</div>

<%= link_to "Add a new meetup", new_meetup_path, class: "btn btn-success" %>

<h2>Click each meetup to see details, add/remove players and game events, or to edit/delete</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    <% @meetups.each do |meetup| %>
      <tr>
        <td><%= link_to meetup.name, meetup_path(meetup) %></td>
        <td><%= meetup.location %></td>
        <td><%= meetup.date.strftime("%b %d, %Y") %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Back home", root_path, class: "btn btn-default" %>
